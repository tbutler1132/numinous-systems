{
  "name": "@numinous-systems/identity",
  "version": "1.0.0",
  "description": "Identity and access control infrastructure",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@numinous-systems/node": "1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
