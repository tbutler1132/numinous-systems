{
  "name": "@vital-systems/root",
  "private": true,
  "workspaces": [
    "core/sensor",
    "sensors/finance",
    "sensors/thought"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces"
  }
}
