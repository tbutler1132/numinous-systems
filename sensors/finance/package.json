{
  "name": "@numinous-systems/finance",
  "version": "1.0.0",
  "description": "Finance sensor - Chase CSV to observation memory",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "finance": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "start": "node dist/cli.js",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@numinous-systems/sensor": "^1.0.0",
    "csv-parse": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
